# SkyLine v20 — persistent cross-device authentication

This update keeps the existing UI and connects authentication to the deployed HTTPS Render API with PostgreSQL persistence. Users created through SkyLine can sign in from another device using the same email and password.
