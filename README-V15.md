# SkyLine v15 — Timeline Login / Cache Fix

- Login now defaults to the current Timeline build when no explicit destination is supplied.
- The login redirect uses a v15 cache-busting query.
- The service worker is upgraded to `skyline-shell-v15`, clears older Skyline shell caches, and uses network-first navigation for HTML.
- The Timeline search icon has more space from the SkyLine logo on desktop and mobile.
- Existing Timeline, wallet, community, theme, profile and navigation behavior is preserved.
