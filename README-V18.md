# SkyLine v18 — Cross-device authentication ready

This build connects the frontend authentication flow to the live HTTPS Render API:
`https://skyline-gmut.onrender.com`

The existing timeline/wallet UI and other project files are retained. Cache/version query strings were advanced to v18 so stale v17 assets are less likely to load.
